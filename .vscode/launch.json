{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "g++", // Tên task
            "type": "shell", // Kiểu task
            "command": "g++", // Lệnh biên dịch
            "args": [
                "${file}", // File nguồn
                "-g", // Thêm thông tin debug
                "-o", // Tên file thực thi
                "D:/C-development/TestandDebug/Pointer_and_Resgister.exe"
            ],
            "group": {
                "kind": "build", // Loại task (build)
                "isDefault": true // Task mặc định
            },
            "problemMatcher": [
                "$gcc" // Sử dụng problem matcher cho g++
            ]
        }
    ]
}